# 道路质量分析优化报告 — 通用模板与话术库

> 基于 4 份实际报告提炼，适用于 LTE/NR 道路遍历性测试优化报告撰写。

---

## 一、报告整体结构

```
1. 封面（标题 + 日期 + 公司）
2. 道路测试优化概述
   - 测试目的
   - 测试工具
   - 测试路线
3. 道路优化指标
   - 指标情况
   - 测试覆盖图（RSRP / SINR 分布图）
4. 问题原因分类汇总
5. 问题点分析（核心区域 ★）
   - 覆盖类
   - 切换类
   - 质差类
6. 测试总结
```

---

## 二、固定话术区域（无需数据支撑，通用性强）

### 2.1 测试目的（固定不变）

> 通过本轮遍历性测试，利用 Assistant 平台分析优化，远近协同，旨在快速提升目标区域路测指标，减少网内干扰，夯实网络质量，提升用户体验。

### 2.2 测试工具（固定结构）

> 测试工具如下表：

| 工具名称 | 用途 |
|---------|------|
| （填入具体工具） | （填入用途） |

### 2.3 问题原因分类汇总（固定过渡）

> 问题原因分类汇总

（此区域放饼图/柱状图，文字较少，话术固定）

---

## 三、★ 问题点分析 — 核心话术模板 ★

问题点分析是报告的核心内容，按问题类型分为三大类。每个问题点由 **问题描述 → 问题分析 → 优化方案** 三段式组成。

---

### 3.1 覆盖类（弱覆盖 / 无覆盖）

#### 3.1.1 标准模板

**标题格式**：`{区域}道路{网络制式}弱覆盖：{具体位置}道路{网络制式}弱覆盖{序号}`

> 示例：阿乌高速道路4G弱覆盖：五家渠入口38km处道路4G弱覆盖1

---

**问题描述模板**：

```
测试车辆行驶至{具体位置}，UE占用小区{小区名称}时，存在{网络制式}弱覆盖{/无覆盖}问题，问题路段持续约{距离}。
```

| 变量 | 说明 | 示例 |
|------|------|------|
| `{具体位置}` | 道路位置描述 | 五家渠入口38km处 |
| `{小区名称}` | 服务小区全名 | CJ_WJQ0_102团8连D_GHCNN_PRT4E_0 |
| `{网络制式}` | 4G / 5G | 4G |
| `{/无覆盖}` | 可选，严重时加上 | /无覆盖 |
| `{距离}` | 问题路段长度 | 8.2km |

---

**问题分析模板（4 种变体）**：

**变体 A — 服务小区自身弱覆盖（最常见）**：
```
UE占用小区{小区名}，RSRP{值}，{补充原因}，导致此路段{网络制式}弱覆盖{/无覆盖}。
```
> 示例：UE占用小区CJ_WJQ0_102团8连D_GHCNN_PRT4E_0时，RSRP-118.88，导致此路段4G弱覆盖。

**变体 B — 近点小区覆盖不足**：
```
UE占用小区{服务小区}时，RSRP{值}，较近小区{邻近小区}对当前道路覆盖较弱，RSRP{值}，导致此路段{网络制式}弱覆盖。
```
> 示例：UE占用小区CJWJQ五家渠103团10连L(R)_JYF1B时，RSRP-105.5，较近小区CJWJQ五家渠103团7连生活区_JYF1C对当前道路覆盖较弱，RSRP-110.75，导致此路段4G弱覆盖。

**变体 C — 山体/建筑物阻挡**：
```
UE占用{小区名}(RSRP:{值})时，{该路段/服务小区与此路段}存在{山体/建筑物}阻挡，导致此路段{网络制式}弱覆盖{/无覆盖}。
```
> 示例：UE占用CJFKS阜康天池观景台_JYF1A(RSRP:-107.88)时，该路段存在山体阻挡，导致此路段4G弱覆盖，无优化空间，建议新建拉远。

**变体 D — 无邻近基站覆盖**：
```
UE占用小区{小区名}时，较近区域无{其他/更优}基站可进行覆盖，{此路段存在山体阻挡/地势低洼}，{补充说明}。
```
> 示例：UE占用小区CJ_HTB0_小甘沟煤矿N_TSDKM_SRH2C_9时较近区域无其他基站可进行覆盖，此路段存在山体阻挡，常态人员较少的问题。

---

**优化方案模板**：

方案按优先级排列，通用措辞：

**已有基站调整类**：
```
1. 调整{小区名}下倾角{原值}°->{目标值}°。
2. 调整{小区名}方位角{原值}°->{目标值}°。
3. 增加{小区名}功率。
4. 核查{小区名}是否存在告警。
```

**邻区优化类**：
```
添加{小区A}到{小区B}双向邻区关系。
```

**新建基站类**：
```
于经纬度: {lng}, {lat} 建设基站{基站名}拉远{小区名}(EARFCN:{值})，小区方位角{值}°，下倾角{值}°。
```

**多方案比选类**：
```
方案一：于经纬度: {lng1}, {lat1} 新建基站……（描述）
方案二：若上述方案较难推动，可尝试……（描述），但由于{原因}，建议谨慎评估此方案。
```

**兜底方案类**：
```
如建设推进困难，可考虑于{位置}建设信号放大器。
```

---

### 3.2 切换类（不切换 / 漏配邻区）

#### 3.2.1 问题描述模板

（与覆盖类相同，问题路段通常较短：几十米到几百米）

```
测试车辆行驶至{具体位置}，UE占用小区{小区名}时，存在{网络制式}弱覆盖{/质差}问题，问题路段持续约{距离}。
```

---

#### 3.2.2 问题分析模板（3 种变体）

**变体 A — 未切换到更优小区（核心话术）**：
```
UE占用小区{服务小区}，未切换至{/未占用}较近{/覆盖更好}小区{目标小区}，进而导致弱覆盖{/质差}。
```
> 示例：UE占用小区CJWJQ五家渠103团14连L_JYH1B，未占用较近且覆盖更好小区CJ_WJQ0_五家渠103团13连D_GHCNN_PRT5C_10，进而导致弱覆盖。

**变体 B — 未切换到信号更好小区**：
```
UE占用小区{服务小区}时，未切换至信号更好小区{目标小区}导致弱覆盖。
```

**变体 C — 跨运营商共享问题**：
```
经工参查询该基站{基站名}站下共计{N}个小区未向我方共享，建议向{运营商}方发起基站{基站名}下小区共享申请。
```

---

#### 3.2.3 优化方案模板

**邻区添加（核心方案）**：
```
1. 添加小区{服务小区}到小区{目标小区}双向邻区关系。
```

**门限调整**：
```
2. 降低小区{小区名}的A2门限。
```

**核查告警/共享**：
```
1. 核查{运营商}是否与我方共享基站{基站名}。
```

---

### 3.3 质差类（SINR差 / 重叠覆盖）

#### 3.3.1 问题描述模板

```
行驶至{具体位置}时，UE占用{运营商}小区{小区名}，存在{网络制式}质差，问题路段持续约{距离}。
```

> 示例：行驶至食品厂小区附近时，UE占用(电信)CJ_WJQ0_青湖名邸楼顶D_GHBXB_PRB2E_0小区，存在4G质差，问题路段持续约360m。

---

#### 3.3.2 问题分析模板（3 种变体）

**变体 A — 无主覆盖小区导致质差**：
```
UE占用{小区名}(距离:{值},RSRP:{值},SINR:{值})时，近点小区{邻近小区}弱覆盖，此路段无主覆盖小区，导致该路段{网络制式}质差。
```
> 示例：UE占用(电信)CJ_WJQ0_青湖名邸楼顶D_GHBXB_PRB2E_0小区(距离:1427.46,RSRP:-98.12,SINR:-3)时，近点小区(联通)CJWJQ五家渠北海公园_FYF1A弱覆盖，此路段无主覆盖小区，导致该路段4G质差。

**变体 B — 重叠覆盖导致质差**：
```
UE占用小区{服务小区}(RSRP:{值})时，RSRP与近点小区{邻近小区}(RSRP:{值})相近，重叠覆盖导致质差。
```
> 示例：UE占用小区(电信)CJ_WJQ0_102团晋泽园西区D_GHCTI_PRT4E_5(RSRP:-84.00)时，RSRP与近点小区(电信)CJ_WJQ0_凤凰茗苑D_GHCTI_PRB2E_2(RSRP:-82.25)相近，重叠覆盖导致质差。

**变体 C — 远点小区弱覆盖导致质差**：
```
UE占用小区{服务小区}(RSRP:{值})时，近点小区{邻近小区}无覆盖，远点小区弱覆盖导致质差。
```

---

#### 3.3.3 解决方案模板

```
1. 调整小区{小区名}方位角{原值}°->{目标值}°。
2. 调整小区{小区名}下倾角{原值}°->{目标值}°。
3. 核查小区{小区名}告警信息。
```

---

## 四、测试总结（固定结构）

```
本轮测试{NR/LTE}覆盖率（（-105&-3）*占用时长占比）{值}%，平均{RSRP/SS-RSRP}为{值}dBm，平均{SINR/SS-SINR}为{值}dB。
分析问题点{N}个，其中弱覆盖问题{n1}个，切换问题{n2}个，质差问题{n3}个，需推动处理后进行下一轮测试。
```

---

## 五、标题命名规范

### 5.1 报告标题

`{区域名称}道路质量分析优化报告`

### 5.2 问题点标题

`{区域}道路{制式}{问题类型}：{具体位置}道路{制式}{问题类型}{序号}`

| 组件 | 选项 | 示例 |
|------|------|------|
| 制式 | 4G / 5G | 4G |
| 问题类型 | 弱覆盖 / 弱覆盖/无覆盖 / 质差 | 弱覆盖 |
| 序号 | 1, 2, 3...（同位置同类问题递增） | 1 |

---

## 六、快速检查清单

- [ ] 测试目的话术是否使用了标准模板
- [ ] 每个问题点是否包含「问题描述→问题分析→优化方案」三段式
- [ ] 问题描述中是否包含：位置 + UE占用小区 + 问题类型 + 持续距离
- [ ] 问题分析中是否包含：RSRP/SINR 数据 + 根因判断
- [ ] 优化方案是否按优先级排列（先调参 → 再加邻区 → 最后新建站）
- [ ] 小区名称是否使用完整工参名称
- [ ] 距离单位是否统一（km / m）
- [ ] 测试总结中是否汇总了所有指标和问题点数量
