# 基站工参管理器 v1.0.1

> 首个正式发布版本 | 免安装绿色单文件，双击即用

---

## 项目简介

基站工参管理器是一个轻量级的 Web 应用，专为**电信/联通基站工参数据管理**设计。支持多运营商、多设备商的工参格式自动识别，提供模糊搜索、分页浏览、多平台导出等一站式功能。

### 技术特点

- **免安装**：PyInstaller 打包为单个 `.exe`，无需安装 Python 或其他依赖
- **便携化**：数据库（cache.json）和数据文件存储在 exe 同目录，可放 U 盘携带
- **局域网可用**：启动后同一局域网其他设备也可访问 `http://IP:18888`

---

## 核心功能

### 1. 智能工参导入

| 运营商 | 制式 | 关键词匹配 |
|--------|------|-----------|
| 中国电信 | 5G NR | `5G汇总工参` / `5G工参` |
| 中国电信 | 4G LTE | `电信工参` / `LTE工参` / `室外宏站` |
| 中国联通 | 5G NR | `5G汇总工参` |
| 中国联通 | 4G LTE | 自动兜底匹配 |

- **多候选列名匹配**：兼容华为、中兴、诺基亚、大唐等不同设备商的命名差异
- **Sheet 预览选择**：上传后可选导入哪些工作表，自动推荐相关 Sheet
- **伪表头过滤**：自动跳过重复表头行和无效数据

### 2. 运营商 & 共享智能检测

基于基站/小区命名规则的三级优先级检测：

| 规则 | 示例 | 判定 |
|------|------|------|
| `CJ_` 开头，5~7 段 | `CJ_WJQ0_102团8连D_GHCNN_PRT4E_0` | 中国电信 |
| `(LTGX)` 标记 | `CJ_QT0_829045奇台二畦基站F_TSCNN_SRT4E-1` | 中国电信（已共享） |
| 联通区县前缀，2 段 | `CJWJQ五家渠北海公园_FYF1A` | 中国联通（自建） |

共享状态以 Excel 列为权威来源：

| Excel 列值 | 结果 |
|-----------|------|
| `是` / `共享` / `电信` | 共享 |
| `否` / `未共享` | 非共享 |

### 3. 搜索 & 浏览

- **模糊搜索**：支持小区名、PCI、基站ID、基站名关键词
- **分页浏览**：支持页码输入框直接跳转
- **搜索结果导出**：所有导出均受搜索过滤影响

### 4. 多平台工参导出

| 导出格式 | 文件 | 用途 |
|---------|------|------|
| 📄 表格 CSV | `.csv` | 通用数据交换 |
| 📡 Pioneer 4G | `.xls` | 鼎利路测软件 |
| 📡 Pioneer 5G | `.xls` | 鼎利路测软件 |
| 📱 Assistant LTE | `.xlsx` | 华为 Assistant 分析平台 |
| 📱 Assistant NR | `.xlsx` | 华为 Assistant 分析平台 |

### 5. 便捷交互

- **列宽拖拽**：表头可拖拽调整列宽
- **单元格点击复制**：点击任意单元格即复制到剪贴板
- **经纬度智能复制**：点击经度或纬度，自动复制 `经度,纬度` 坐标对
- **Ctrl+A 全局搜索**：任意位置按 Ctrl+A 即聚焦搜索框
- **Escape 清空搜索**：Esc 键清空搜索框
- **全局键盘输入**：任意可打印字符自动跳转搜索框

### 6. 数据管理

- **文件/工作表级移除**：可单独移除某个文件或某个 Sheet 的数据
- **一键清空**：清空所有已导入数据
- **自动缓存**：关闭时自动保存 `cache.json`，启动时自动恢复

---

## 快速开始

### 方式一：双击 exe

1. 下载 `基站工参管理器.exe`
2. 双击运行
3. 浏览器自动打开 `http://127.0.0.1:18888`

### 方式二：源码运行

```bash
pip install pandas openpyxl xlwt
python app.py
```

---

## 系统要求

| 项目 | 要求 |
|------|------|
| 操作系统 | Windows 10+ |
| 端口 | 18888（启动时自动释放占用） |
| 浏览器 | Chrome / Edge / Firefox |

---

## 变更日志（v1.0.1）

### 🐛 修复
- 电信 4G「共享方」列值 `"电信"` 正确识别为"共享"
- 「共享方」列值 `"未共享"` 正确识别为"非共享"

### ✨ 首次发布功能
- 4 套字段映射规则（电信5G/4G + 联通5G/4G）
- 智能运营商归属检测
- 5 种导出格式（CSV + Pioneer ×2 + Assistant ×2）
- 文件卡片单行横向滚动
- 分页页码跳转
- 经纬度坐标对复制
- 启动脚本三阶段诊断
- PyInstaller 单 exe 分发

---

## 下载

[基站工参管理器 v1.0.1](https://github.com/Xiaomo3250/CellSearchTool/releases/tag/v1.0.1)

---

**🚀 基站工参管理从未如此简单。**
