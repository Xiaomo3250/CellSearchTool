# 基站工参管理器 v2.0.2

基站工参速查工具，支持 PC 端（Web / 单文件 EXE）和 Android 手机端。

## 功能特性

- **多字段组合搜索** — 制式、TAC、基站ID、CellID、PCI、频点、基站名、小区名任意组合
- **智能字段识别** — 兼容电信/联通/移动 4G/5G 工参，自动适配 SSB绝对信道号、EARFCN、频点等不同列名
- **运营商自动检测** — 六级回退：承建方列 → 文件名 → 关键字 → 小区名前缀(CJ_→电信) → 区县前缀 → 制式兜底
- **小区ID 规范化** — 联通4G 长格式(基站ID+小区标识)自动截短，旧数据自动迁移
- **一键复制** — 小区名 / ID / 经纬度，点一下即复制
- **设备商识别** — 自动映射厂家列，卡片和表格均显示设备商标签
- **分 Sheet 导入** — 多 Sheet 勾选，全选/全不选一键切换
- **数据管理** — 按文件查看/删除已导入数据
- **黑夜模式** — 手机端一键切换，偏好自动记住
- **PC 端单文件** — 一个 exe 免装 Python，双击即用
- **手机离线版** — APK 装完无需网络，微信/QQ 文件直接导入

## 版本说明

| 版本 | 形式 | 说明 |
|------|------|------|
| 🖥 PC 端 | `CellSearchTool.exe` | 单文件，双击启动，浏览器访问 localhost:18888 |
| 📱 Android | APK 安装包 | arm64-v8a(主流) / armeabi-v7a(老旧) / x86_64(模拟器) |

## 支持的工参格式

已覆盖以下运营商/制式组合的字段命名规范：

| 运营商 | 制式 | 频点列名示例 | 小区ID列名示例 |
|--------|------|-------------|---------------|
| 中国电信 | 5G NR | SSB绝对信道号 | 小区ID、NR小区标识 |
| 中国电信 | 4G LTE | 下行中心频点号、EARFCN | CELLID、本地CellID |
| 中国联通 | 4G LTE | 频点、EARFCN | 小区ID、CellID、小区标识 |
| 中国联通 | 5G NR | SSB频点 | NR小区标识、小区标识 |
| 乌鲁木齐 | 4G LTE | EARFCN | 小区ID |

## 项目结构

```
├── app.py              # PC 端主程序 (Flask + Web UI)
├── db.py               # SQLite 数据库层
├── kml_export.py       # KML 基站图层导出
├── field_app/          # Flutter 移动端源码
│   └── lib/
│       ├── main.dart           # 主界面 + 搜索 + 卡片
│       ├── db/database.dart    # SQLite 层
│       ├── db/importer.dart    # xlsx 解析 + 字段映射
│       ├── models/station.dart # 数据模型
│       └── pages/              # 数据管理页
└── build/              # 构建产物目录
```
| 下倾角 | 总下倾角 |
| 挂高 | 天线挂高 |
| 方位角 | 天线方位角 |
| 经度 | 站点经度 |
| 纬度 | 站点纬度 |

## 快速开始

### 环境要求

- Python 3.8+
- pandas

### 安装依赖

```bash
pip install pandas openpyxl
```

### 启动应用

```bash
cd 0423-工参管理器
python app.py
```

启动后自动打开浏览器访问 `http://127.0.0.1:18888`

### 使用方式

1. **自动导入** — 将工参 xlsx 文件放入 `../0422/工参/` 目录，启动时自动加载
2. **手动上传** — 点击页面上方的「选择文件」按钮上传工参文件
3. **搜索** — 在搜索框中输入小区名或 ECI，按回车搜索
4. **浏览** — 在基站目录标签页中分页浏览全部记录
5. **导出** — 搜索结果可点击「导出 CSV」下载

## 项目结构

```
0423-工参管理器/
├── app.py              # 主应用（后端 + 前端 HTML）
├── uploaded_files/     # 手动上传的工参文件存储目录
└── README.md
```

整个应用是一个单文件 Python 程序，无需额外的前端构建工具或数据库。

## 技术实现

- **后端**：Python 内置 `http.server`，线程安全的数据操作（`threading.Lock`）
- **前端**：内嵌 HTML + CSS + JavaScript，响应式布局
- **数据解析**：pandas 读取 Excel，手动解析 multipart 上传（兼容 Windows）
- **字段映射**：规则引擎 + 多候选列名优先级匹配，具备容错空间

## 端口配置

默认端口 `18888`，如需修改请在 `app.py` 底部的 `PORT` 变量中更改。

## 注意事项

- 首次启动导入大量工参文件时，数据加载需要数十秒，页面会显示加载进度
- 文件名需包含运营商关键词（电信 / 联通 / 移动）和技术关键词（5G / 4G / LTE / NR）以便自动识别格式
- 清空数据后可重新上传工参文件，不影响后续使用
